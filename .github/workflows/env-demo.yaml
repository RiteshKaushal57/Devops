name: Env Demo
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  env_demo:
    env:
      APP_ENV: "prod"
    runs-on: ubuntu-latest
    steps:
      - name: Introducing env variables
        run: echo "App is connecting to $APP_ENV!"